# Copy this file to .env and fill in your real key. .env is gitignored.

# Required to enable real LLM reasoning in evaluate_customer().
# If unset, the app falls back to the deterministic heuristic (no API needed).
OPENAI_API_KEY=

# Optional, defaults to gpt-4o-mini.
OPENAI_MODEL=gpt-4o-mini
